// Forca a entrada para a leitura dos caminhos par entrar na configuracao do sequelize

const path = require('path');

module.exports ={
    config: path.resolve(__dirname, 'src', 'config', 'database.js')
};